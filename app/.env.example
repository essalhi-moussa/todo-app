# Copier en .env pour un lancement local (ce fichier ne contient aucun secret).
NODE_ENV=development
PORT=3000
APP_VERSION=dev

# STORE_DRIVER=memory permet de démarrer sans PostgreSQL.
STORE_DRIVER=postgres

DB_HOST=localhost
DB_PORT=5432
DB_NAME=tododb
DB_USER=todo
DB_PASSWORD=change-me
