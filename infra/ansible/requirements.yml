---
# Collections Ansible utilisees par les roles du projet.
# Installation : ansible-galaxy collection install -r requirements.yml
collections:
  # modprobe, timezone, git_config
  - name: community.general
    version: ">=8.0.0"
  # sysctl
  - name: ansible.posix
    version: ">=1.5.0"
